{
  "name": "@office/sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "smoke": "npm run build && node test/smoke.mjs",
    "bundle": "esbuild dist/index.js --bundle --platform=node --format=esm --outfile=bundle/engine.mjs --log-level=warning"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@office/contracts": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
